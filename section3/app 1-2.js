// Built In Constructors

// Strings 

const name1 = 'Matt';
const name2 = new String('Matt');

console.log(typeof name1, name2);

//  Getting objects for name2 despite the values being the same.

// Easier and faster to use non built in constructors but good to see.